spring.data.mongodb.uri=mongodb://localhost:27017/test
# Test profile defaults — Mongo is provided by @ServiceConnection (Testcontainers).
# Inventory Feign client still needs a URL at context startup.
inventory.service.url=http://localhost:8081

# Avoid failing health checks that depend on external systems during tests
management.health.mongo.enabled=true
