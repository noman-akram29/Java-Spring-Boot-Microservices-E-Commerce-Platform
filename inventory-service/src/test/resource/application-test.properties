# Intentionally empty — datasource is provided by the @ServiceConnection
# Testcontainers MySQLContainer in TestcontainersConfiguration.java.
# Do NOT add localhost/root fallback values here: a missing container
# must fail loudly, not silently connect to a real local database.