trigger: none

pool:
  vmImage: ubuntu-latest

steps:
  - task: AWSCLI@1
    displayName: "Test AWS OIDC authentication"
    inputs:
      awsCredentials: "AWS-ECR-OIDC"
      regionName: "ap-south-1"
      awsCommand: "sts"
      awsSubCommand: "get-caller-identity"