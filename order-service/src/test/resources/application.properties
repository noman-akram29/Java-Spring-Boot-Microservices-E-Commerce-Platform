# MySQL + Kafka come from @ServiceConnection (Testcontainers)
# Inventory Feign client points at WireMock (@AutoConfigureWireMock)
inventory.service.url=http://localhost:${wiremock.server.port:8089}
spring.flyway.enabled=true