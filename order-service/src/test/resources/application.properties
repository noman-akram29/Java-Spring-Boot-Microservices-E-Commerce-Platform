# BUG FIX: the Feign client reads "inventory.service.url" (see InventoryClient.java /
# application.properties), not "inventory.url". The old key silently did nothing.
inventory.service.url=http://localhost:${wiremock.server.port}

# Keep the outbox publisher from firing on its own schedule mid-test
spring.task.scheduling.enabled=false